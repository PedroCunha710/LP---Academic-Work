# Build artifacts (NetBeans/Cygwin toolchain)
build/
dist/
*.o
*.o.d
*.exe

# Build artifacts (MSVC / VS Code cl.exe task, used only for local test-compiles)
*.obj
*.pdb
*.ilk

# NetBeans private project data (machine-specific)
nbproject/private/

# Data files generated at runtime (binary persistence)
*.bin

# Logs
*.log
